//create a function that takes in two num params and add them together and returns the sum
let add = (numOne, numTwo) => {
  return numOne + numTwo
};


let times = (param1, param2) => {
  return param1 * param2
};
